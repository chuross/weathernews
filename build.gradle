buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.12.2'
    }
}

allprojects {
    repositories {
        jcenter()
        maven { url 'http://chuross.github.com/maven-repository/' }
    }
}
